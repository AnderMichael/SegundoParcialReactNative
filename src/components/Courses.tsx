import {StyleSheet, Text, View} from 'react-native';
import {globalStyles} from '../theme/global';
import {Section} from './Section';

export const Courses = () => {
  return <Section titleSection="Popular course" flexSection={4}>

  </Section>;
};
